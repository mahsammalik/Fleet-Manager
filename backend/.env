# Port for backend API
PORT=4100

# PostgreSQL connection string
# Replace user, password, host, port, and dbname with your actual values
DATABASE_URL=postgres://fleetadmin:FleetPass123@localhost:5432/fleetmanager

# JWT secret key (use a long random string in real projects)
JWT_SECRET=trs-dev-secret-change-in-prod